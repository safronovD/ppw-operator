name: Ppw image

on:
  push:
    branches: [dev]

env:
  IMAGE_NAME: ppw-operator
  VERSION: latest
  IMAGE_ID: docker.pkg.github.com/safronovD/ppw-operator/$IMAGE_NAME
  
jobs:
  dockerize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        
      - name: Build image
        run: docker build . -f Dockerfile -t $IMAGE_NAME

      - name: Log into registry
        run: echo "${{ secrets.GITPKGS_PASSWORD }}" | docker login docker.pkg.github.com -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: |
          IMAGE_ID=$(echo $IMAGE_ID | tr '[A-Z]' '[a-z]')
          docker tag $IMAGE_NAME $IMAGE_ID:$VERSION
          docker push $IMAGE_ID:$VERSION